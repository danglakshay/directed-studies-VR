using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GestureToAttach : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

	void isThisRunning(){
		Debug.Log("Woah script has been triggered");

	}

    // Update is called once per frame
    void Update()
    {
        
    }
}
